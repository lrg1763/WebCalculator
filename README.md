# WebCalculator

Простой веб-калькулятор на HTML, CSS и JavaScript.

## Запуск

Откройте `index.html` в браузере или используйте локальный сервер.

## Структура

- `index.html` — разметка
- `style.css` — стили
- `script.js` — логика калькулятора
- `fonts/` — шрифты
- `favicon_io/` — иконка
